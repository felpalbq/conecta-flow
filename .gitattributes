# Normalize line endings to LF regardless of platform/git config — avoids
# perpetual CRLF/LF drift between Windows checkouts and Prettier (which
# defaults to LF), and keeps diffs clean across contributors.
* text=auto eol=lf

*.ico binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
